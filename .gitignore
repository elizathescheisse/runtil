.DS_Store
.build/
DerivedData/
.swiftpm/

# Per-machine Xcode state: open tabs, breakpoints, window layout. Nothing another
# checkout would want, and it changes constantly.
xcuserdata/
*.xcuserstate
*.xcscmblueprint

# Per-machine signing config (team ID). Copy from Local.xcconfig.example.
Local.xcconfig
